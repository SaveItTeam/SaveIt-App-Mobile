<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Icone selecionado -->
    <item android:color="@color/md_theme_primary" android:state_checked="true" />
    <!-- Icone desabilitado -->
    <item android:color="@color/PlaceHolderColor" android:state_enabled="false" />
    <!-- Icone normal -->
    <item android:color="@color/md_theme_onSurface" />
</selector>